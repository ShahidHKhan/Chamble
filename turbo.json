{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"], "env": ["VITE_SERVER_URL"] },
    "dev":   { "cache": false, "persistent": true },
    "lint":  {}
  }
}
